// There are mainly 7 primitive data types in JS.
// NN BB SS U

let a = null; // Null value
let b = 567; // Integer value
let c = true; // can be false.
let d = BigInt("567"); // BigInt value
let e = "Krypto"; // String
let f = Symbol("I'm a symbol!"); // Symbol
let g = undefined; // undefined, a variable is itself previously is undefined.

console.log(a, b, c, d, e, f);
